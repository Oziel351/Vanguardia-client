import { Box } from "@mui/material";
import { DrawerContent } from "../components/DrawerContent";

export const Home = () => {
  return (
    <Box display="flex">
      <DrawerContent />
    </Box>
  );
};
